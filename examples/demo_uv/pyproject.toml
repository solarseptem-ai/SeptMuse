[project]
name = "septmuse-demo"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "septmuse[openai]",
    "qdrant-client",
]

[tool.uv.sources]
septmuse = { path = "../..", editable = true }